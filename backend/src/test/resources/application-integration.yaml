server:
  port: 0  # Random port for testing

spring:
  application:
    name: blog-counter-test
  task:
    scheduling:
      enabled: false
    execution:
      enabled: false

scheduler:
  enabled: false

logging:
  level:
    com.example.blogcounter: DEBUG
    org.springframework.web.socket: DEBUG
    org.springframework.messaging.simp.stomp: DEBUG
